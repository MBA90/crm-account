<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Consent attaches to the person (contact), not the company (account). -->
    <changeSet id="007-create-consents-table" author="crm">
        <createTable tableName="consents">
            <column name="consent_id" type="UUID">
                <constraints primaryKey="true" primaryKeyName="pk_consents" nullable="false"/>
            </column>
            <column name="contact_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="channel" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="purpose" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="granted" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="source" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <!-- Evidence document in another service (workflow attachments); plain UUID, no FK here. -->
            <column name="evidence_ref" type="UUID"/>
            <column name="granted_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="withdrawn_at" type="TIMESTAMP WITH TIME ZONE"/>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="consents"
                baseColumnNames="contact_id"
                referencedTableName="contacts"
                referencedColumnNames="contact_id"
                constraintName="fk_consents_contact"
                onDelete="CASCADE"/>

        <!-- One consent per person, channel and purpose. -->
        <addUniqueConstraint
                tableName="consents"
                columnNames="contact_id, channel, purpose"
                constraintName="uq_consents_contact_id_channel_purpose"/>

        <createIndex tableName="consents" indexName="idx_consents_contact_id">
            <column name="contact_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
